package ${packageEntity.service};

#foreach(${importEach} in ${importListEntity.primaryServiceList})
import ${importEach};
#end

/**
 * @author : ${authEntity.authName}
 * description : [ ${tableEntity.beanComment} ] 服务类基础接口
 * date : ${authEntity.buildDate}
 * email : ${authEntity.authEmail}
 */
public interface ${nameEntity.service} extends IBasePrimaryService<${nameEntity.entity}> {

}